# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local
*.tsbuildinfo

# Crawl working state — regenerable; the published snapshots live in src/data/.
# Leading slash anchors this to the repo root so it does NOT match src/data/.
/data/

# Optional trade-site credentials for `npm run price` (POESESSID / CF_CLEARANCE).
.env
.env.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.claude/

# Regenerable diagnostics and real-browser evidence. CI uploads these directories
# when a run needs to be retained; they are not source inputs or release artifacts.
/output*.txt
/quality-lab/artifacts/
/quality-lab/reports/
