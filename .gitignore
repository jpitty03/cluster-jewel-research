# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local
*.tsbuildinfo

# Crawl working state — regenerable; the published snapshots live in src/data/.
# Leading slash anchors this to the repo root so it does NOT match src/data/.
/data/

# Optional trade-site credentials for `npm run price` (POESESSID / CF_CLEARANCE).
.env
.env.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.claude/

# Real-browser videos, traces, and transient run directories. Stable evidence is
# copied to quality-lab/reports/evidence/ by the release harness.
/quality-lab/artifacts/
